# Area-Specific Agent Instructions

This file is intended to be copied as `AGENTS.md` into a major project area such
as `frontend/` or `backend/`.

## Scope

These instructions apply only to files under `{{DIRECTORY}}/`.

## Required context

- `{{RELEVANT_ARCHITECTURE_DOCUMENT}}`
- `{{RELEVANT_ENGINEERING_DOCUMENT}}`

## Commands

- Develop: `{{COMMAND}}`
- Test: `{{COMMAND}}`
- Lint: `{{COMMAND}}`
- Build: `{{COMMAND}}`

## Area-specific rules

- {{RULE}}

Root `AGENTS.md` instructions still apply. These instructions may specialize them
for this directory but must not silently contradict repository-wide security or
documentation rules.
